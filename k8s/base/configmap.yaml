apiVersion: v1
kind: ConfigMap

metadata:
  name: cloudcart-config
  namespace: cloudcart

data:
  NODE_ENV: "production"
  PORT: "5000"

  # Backend Service DNS inside Kubernetes
  BACKEND_URL: "http://backend-service:5000"

  # Frontend URL
  FRONTEND_URL: "http://cloudcart.local"